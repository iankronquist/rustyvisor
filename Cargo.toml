[package]
name = "rustyvisor"
version = "0.1.0"
authors = ["Ian Kronquist <iankronquist@gmail.com>"]


[dependencies]
spin = {version = "0.4"}
allocator = {path = "libs/allocator"}

[features]
runtime_tests = []

[lib]
crate_type = ["staticlib"]
